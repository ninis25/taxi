# Variables d'environnement (clés API, mots de passe)
.env
.env.local
.env.*.local
back/.env

# Dépendances
node_modules/

# Logs et cache
npm-debug.log*
*.log
.DS_Store

# Les images (front/images/, images/) ne sont PAS ignorées → elles partent au push
